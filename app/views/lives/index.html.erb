<div class="menu justify-center x-4 w-14 flex-none">
  <div class="bg-gray-300 h-screen px-4 pt-12">
    <% @lives.each do |live| %>
      <div class="menu_i hover:text-white pt-3 pb-2 uppercase animate__animated animate__fadeInDown"><%= link_to live.abbreviation, life_path(live), class:"menu_i_tx over:text-white" %></div>
    <% end %>
    <div class="menu_i hover:text-yellow-500"><span class="menu_i_tx"><%= link_to "✕", phases_path %></span></div>
  </div>
</div>


<!-- <div class="left flex-grow">
  <div class="bg-gray-100 h-screen px-12 py-12">
    <% @lives.each do |live| %>
      <h1 class="text-4xl hover:text-yellow-500 uppercase animate__animated animate__fadeInDown"><%= link_to live.title, life_path(live) %></h1>
    <% end %>
  </div>
</div>
 -->
